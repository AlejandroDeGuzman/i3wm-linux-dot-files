#!/usr/bin/env bash

# Terminate already running bar instances
polybar-msg cmd quit

# Launch bar1 (replace with your bar name)
polybar bar1 &
#!/usr/bin/env bash

# Terminate already running bar instances
polybar-msg cmd quit
